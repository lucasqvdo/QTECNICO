import { useEffect, useMemo, useState, type ReactNode } from 'react';
import { BarChart3, CheckCircle2, ClipboardList, DollarSign, TrendingUp, Users, Wrench } from 'lucide-react';
import { Bar, BarChart, CartesianGrid, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis, Cell } from 'recharts';
import { api } from './api';
import { getOfflineSnapshot } from './offlineStore';
import AdminShellV2, { type AdminSectionV2 } from './AdminShellV2';
import TeamManagement from './TeamManagement';
import ClientsManagement from './ClientsManagement';
import OrdersManagement from './OrdersManagement';
import AttendanceManagement from './AttendanceManagement';
import AgendaManagement from './AgendaManagement';
import FinanceManagement from './FinanceManagement';
import CompanyProfile from './CompanyProfile';
import SubscriptionBilling from './SubscriptionBilling';
import type { Client, OrderStatus, ServiceOrder } from './types';

type DashboardSummary={period:string;stats:{total:number;pending:number;inProgress:number;completed:number;cancelled:number;revenue:number;paid:number;costs:number;margin:number;clients:number;newClients:number};monthly:{month:string;revenue:number;orders:number}[];technicians:{id:number;name:string;orders:number;completed:number;revenue:number}[];recent:{id:string;client:string;type:string;status:OrderStatus;date:string;value:number;technician:string|null}[]};
const COLORS=['#f59e0b','#3b82f6','#10b981','#ef4444'];
const STATUS:Record<OrderStatus,{label:string;className:string}>={pending:{label:'Pendente',className:'bg-amber-100 text-amber-700'},in_progress:{label:'Em andamento',className:'bg-blue-100 text-blue-700'},completed:{label:'Concluída',className:'bg-emerald-100 text-emerald-700'},cancelled:{label:'Cancelada',className:'bg-red-100 text-red-700'}};
const money=(v:number)=>v.toLocaleString('pt-BR',{style:'currency',currency:'BRL'});

function Stat({icon:Icon,label,value,detail}:{icon:typeof ClipboardList;label:string;value:string|number;detail:string}){return <div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-start justify-between gap-3"><div><p className="text-sm font-medium text-slate-500">{label}</p><p className="mt-2 text-2xl font-bold tracking-tight text-slate-900">{value}</p><p className="mt-1 text-xs text-slate-500">{detail}</p></div><div className="rounded-xl bg-cyan-50 p-3 text-cyan-600"><Icon size={21}/></div></div></div>}

function Dashboard(){
 const[period,setPeriod]=useState('30'),[summary,setSummary]=useState<DashboardSummary|null>(null),[loading,setLoading]=useState(true),[error,setError]=useState('');
 useEffect(()=>{let mounted=true;setLoading(true);setError('');void api.getDashboardSummary(period).then(data=>{if(mounted)setSummary(data)}).catch(e=>{if(mounted)setError(e instanceof Error?e.message:'Não foi possível carregar o dashboard.')}).finally(()=>{if(mounted)setLoading(false)});return()=>{mounted=false}},[period]);
 const statusData=useMemo(()=>summary?[{name:'Pendentes',value:summary.stats.pending},{name:'Em andamento',value:summary.stats.inProgress},{name:'Concluídas',value:summary.stats.completed},{name:'Canceladas',value:summary.stats.cancelled}].filter(x=>x.value>0):[],[summary]);
 const s=summary?.stats;
 return <div className="p-4 sm:p-6 lg:p-8"><div className="mb-7 flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="text-sm font-medium text-cyan-600">Visão geral</p><h1 className="mt-1 text-3xl font-bold tracking-tight">Dashboard</h1><p className="mt-1 text-sm text-slate-500">Indicadores da operação, clientes e resultado financeiro.</p></div><select value={period} onChange={e=>setPeriod(e.target.value)} className="w-fit rounded-xl border border-slate-200 bg-white px-3 py-2 text-sm"><option value="7">Últimos 7 dias</option><option value="30">Últimos 30 dias</option><option value="90">Últimos 90 dias</option><option value="all">Todo o período</option></select></div>{error&&<div className="mb-5 rounded-xl border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">{error}</div>}{loading||!s?<div className="rounded-2xl border border-slate-200 bg-white p-12 text-center text-slate-500">Carregando indicadores...</div>:<><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Stat icon={ClipboardList} label="Ordens" value={s.total} detail={`${s.pending} pendentes · ${s.inProgress} em andamento`}/><Stat icon={CheckCircle2} label="Concluídas" value={s.completed} detail={`${s.cancelled} canceladas`}/><Stat icon={DollarSign} label="Faturamento" value={money(s.revenue)} detail={`${money(s.paid)} recebido`}/><Stat icon={TrendingUp} label="Margem operacional" value={money(s.margin)} detail={`Custos: ${money(s.costs)}`}/></section><section className="mt-5 grid gap-5 xl:grid-cols-[1.7fr_1fr]"><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-bold">Evolução financeira</h2><p className="text-xs text-slate-500">Dados agregados pelo servidor</p><div className="mt-4 h-64">{summary.monthly.length?<ResponsiveContainer width="100%" height="100%"><BarChart data={summary.monthly}><CartesianGrid strokeDasharray="3 3" vertical={false}/><XAxis dataKey="month"/><YAxis/><Tooltip formatter={v=>money(Number(v))}/><Bar dataKey="revenue" fill="#22c5df" radius={[7,7,0,0]}/></BarChart></ResponsiveContainer>:<div className="flex h-full items-center justify-center text-sm text-slate-500">Sem dados no período.</div>}</div></div><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-bold">Status das ordens</h2><p className="text-xs text-slate-500">Distribuição do período</p><div className="h-64">{statusData.length?<ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={statusData} dataKey="value" nameKey="name" cx="50%" cy="50%" innerRadius={55} outerRadius={85} paddingAngle={3}>{statusData.map((item,i)=><Cell key={item.name} fill={COLORS[i%COLORS.length]}/>)}</Pie><Tooltip/></PieChart></ResponsiveContainer>:<div className="flex h-full items-center justify-center text-sm text-slate-500">Nenhuma ordem.</div>}</div></div></section><section className="mt-5 grid gap-5 lg:grid-cols-2"><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><div><h2 className="font-bold">Equipe</h2><p className="text-xs text-slate-500">Produtividade no período</p></div><Wrench size={19} className="text-cyan-600"/></div><div className="mt-4 space-y-3">{summary.technicians.length?summary.technicians.map(t=><div key={t.id} className="flex items-center justify-between rounded-xl bg-slate-50 px-4 py-3"><div><p className="font-semibold">{t.name}</p><p className="text-xs text-slate-500">{t.orders} OS · {t.completed} concluídas</p></div><span className="text-sm font-bold">{money(t.revenue)}</span></div>):<p className="text-sm text-slate-500">Nenhum técnico cadastrado.</p>}</div></div><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><div><h2 className="font-bold">Clientes</h2><p className="text-xs text-slate-500">Base comercial</p></div><Users size={19} className="text-cyan-600"/></div><div className="mt-5 grid grid-cols-2 gap-3"><div className="rounded-xl bg-slate-50 p-4"><p className="text-xs text-slate-500">Clientes</p><p className="mt-1 text-2xl font-bold">{s.clients}</p></div><div className="rounded-xl bg-slate-50 p-4"><p className="text-xs text-slate-500">Novos no mês</p><p className="mt-1 text-2xl font-bold">{s.newClients}</p></div></div></div></section><section className="mt-5 rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-100 p-5"><h2 className="font-bold">Ordens recentes</h2><p className="text-xs text-slate-500">Resumo sem carregar fotos, assinaturas ou atendimentos</p></div><div className="overflow-x-auto"><table className="w-full text-sm"><thead className="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th className="px-5 py-3">OS</th><th className="px-5 py-3">Cliente</th><th className="px-5 py-3">Serviço</th><th className="px-5 py-3">Técnico</th><th className="px-5 py-3">Status</th><th className="px-5 py-3 text-right">Valor</th></tr></thead><tbody className="divide-y divide-slate-100">{summary.recent.map(o=><tr key={o.id}><td className="px-5 py-3.5 font-semibold">{o.id}</td><td className="px-5 py-3.5">{o.client}</td><td className="px-5 py-3.5 text-slate-600">{o.type}</td><td className="px-5 py-3.5 text-slate-600">{o.technician||'Não atribuído'}</td><td className="px-5 py-3.5"><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${STATUS[o.status].className}`}>{STATUS[o.status].label}</span></td><td className="px-5 py-3.5 text-right font-semibold">{money(o.value)}</td></tr>)}</tbody></table></div></section></>}</div>
}

export default function AdminDashboardV2(){const[section,setSection]=useState<AdminSectionV2>('dashboard'),[orders,setOrders]=useState<ServiceOrder[]>([]),[clients,setClients]=useState<Client[]>([]),[moduleLoading,setModuleLoading]=useState(false),[error,setError]=useState('');useEffect(()=>{if(section==='dashboard'||section==='team'||section==='company'||section==='billing')return;let mounted=true;setModuleLoading(true);setError('');const load=async()=>{try{const [o,c]=await Promise.all([api.getOrders(),api.getClients()]);if(mounted){setOrders(Array.isArray(o)?o:[]);setClients(Array.isArray(c)?c:[])}}catch(error){if(!mounted)return;try{const cached=await getOfflineSnapshot();if(cached.orders.length||cached.clients.length){setOrders(cached.orders);setClients(cached.clients);setError('')}else setError(error instanceof Error?error.message:'Não foi possível carregar os dados desta área.')}catch(cacheError){setError(error instanceof Error?error.message:cacheError instanceof Error?cacheError.message:'Não foi possível carregar os dados desta área.')}}finally{if(mounted)setModuleLoading(false)}};void load();return()=>{mounted=false}},[section]);let content:ReactNode;if(section==='dashboard')content=<Dashboard/>;else if(section==='company')content=<CompanyProfile onBack={()=>setSection('dashboard')}/>;else if(section==='team')content=<TeamManagement onBack={()=>setSection('dashboard')}/>;else if(section==='billing')content=<SubscriptionBilling onBack={()=>setSection('dashboard')}/>;else if(moduleLoading)content=<div className="p-8 text-center text-slate-500">Carregando área administrativa...</div>;else if(section==='clients')content=<ClientsManagement clients={clients} orders={orders} onClientsChange={setClients} onBack={()=>setSection('dashboard')}/>;else if(section==='orders')content=<OrdersManagement orders={orders} clients={clients} onOrdersChange={setOrders} onBack={()=>setSection('dashboard')}/>;else if(section==='attendances')content=<AttendanceManagement orders={orders} onOrdersChange={setOrders} onBack={()=>setSection('dashboard')}/>;else if(section==='agenda')content=<AgendaManagement orders={orders} onOrdersChange={setOrders} onBack={()=>setSection('dashboard')}/>;else content=<FinanceManagement orders={orders} onOrdersChange={setOrders} onBack={()=>setSection('dashboard')}/>;return <AdminShellV2 section={section} onSectionChange={setSection}>{error&&section!=='dashboard'?<div className="mx-4 mt-4 rounded-xl border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">{error}</div>:null}{content}</AdminShellV2>;
}
